import React, { FC } from 'react';
import { Container } from './styles';

const App: FC = () => (
  <Container>
    Hey, it is the DevTools app running on React.js
  </Container>
);

export default App;
